# Sanity project ID from https://sanity.io/manage (or `npx sanity init --bare`).
PUBLIC_SANITY_PROJECT_ID=""
PUBLIC_SANITY_DATASET="production"

# Only needed for `npm run seed`. Create a token with Editor permissions at
# sanity.io/manage -> API -> Tokens. Never commit this value.
SANITY_WRITE_TOKEN=""
